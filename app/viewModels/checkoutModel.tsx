import React from "react";
import CheckoutScreen from "../views/checkoutScreen/checkout";

const CheckoutModel = ({ navigation }: any) => {
    return (
        <CheckoutScreen navigation={navigation} />
    )
}

export default CheckoutModel