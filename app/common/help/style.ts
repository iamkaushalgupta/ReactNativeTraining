import { Image, StyleSheet } from "react-native"

const styles = StyleSheet.create({
    rightlogo: {
         marginRight: 20,
        resizeMode: 'contain',
        tintColor: 'white',
    }

})
export default styles;
