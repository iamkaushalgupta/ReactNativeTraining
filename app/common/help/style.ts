import { Image, StyleSheet } from "react-native"

const styles = StyleSheet.create({
    rightlogo: {
        margin: 20,
        resizeMode: 'contain',
        tintColor: 'white',
    }

})
export default styles;
